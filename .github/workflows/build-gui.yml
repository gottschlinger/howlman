name: Build GUI

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Debug icon path
        run: mvn -P gui help:evaluate -Dexpression=jpackage.icon.file -q -DforceStdout

      - name: Check icon file exists
        run: ls src/main/resources/gottschlinger/howlman/gui/

      - name: Build GUI app image
        run: mvn -P gui verify -DskipTests

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: howlman-${{ matrix.os }}
          path: target/dist/
